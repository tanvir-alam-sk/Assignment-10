python3 -m venv .venv
source .venv/bin/activate  

source .venv/Scripts/activate
___________________________________________
python.exe -m pip install --upgrade pip

pip install -r requirements.txt


===============================================

python manage.py migrate

python manage.py createsuperuser
python manage.py runserver

_______________________________________
# First make sure Ollama is running in a separate terminal:

ollama pull gemma2:2b

# Make sure Ollama is running in a separate terminal:
ollama run gemma2:2b
_______________________________________
# In another terminal, activate your virtual environment and run the commands in sequence:

# For property title rewriting:
python manage.py rewrite_titles --batch-size 2

# 1. Generate descriptions for all hotels
python manage.py generate_descriptions --batch-size 2

# 2. Generate summaries using the descriptions
python manage.py generate_summaries --batch-size 2

# 3. Generate reviews
python manage.py generate_reviews --batch-size 2

__________________________________________________
# Generate reviews for hotels without them
python manage.py generate_reviews --batch-size 2

# Force regenerate reviews for all hotels
python manage.py generate_reviews --batch-size 2 --force
